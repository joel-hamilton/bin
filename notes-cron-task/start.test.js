describe("start", () => {
  beforeEach(() => {
    //
  });

  it("throws error if missing env variables", () => {
    // TODO, export async module, and also call it if being called from cmdline
  });
});
